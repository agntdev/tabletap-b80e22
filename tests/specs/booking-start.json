[
  {
    "name": "Book table",
    "steps": [
      {
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "Begin a new booking flow (party size → date → time → optional contact → confirm)"
            }
          }
        ],
        "send": {
          "callback": "booking:start"
        }
      }
    ]
  }
]
